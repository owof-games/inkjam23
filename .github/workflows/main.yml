name: Actions 😎

on: [push, workflow_dispatch]

jobs:
  build-and-upload:
    uses: ./.github/workflows/build-and-upload.yml
    with:
      project_name: ${{ vars.PRIVATE_PROJECT_NAME }}
      excludes_windows: true
      excludes_macos: true
      excludes_linux: true
    secrets:
      BUTLER_CREDENTIALS: ${{ secrets.BUTLER_CREDENTIALS }}